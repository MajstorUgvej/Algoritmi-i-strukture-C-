#include <iostream>
#include <vector>

/*
	comparisons: n
	swaps: n?
	adaptive: no
	stable: no
	extra memory: Auxilary array(max(n)) - i jos vise
*/


struct node
{
	int val;
	node* next;

	node(int val) : val(val), next(nullptr) {};
	node() : val(0), next(nullptr) {};
};

// time: O(n), space: O(n)
void Bin_Bucket_Sort(int niz[], int n)
{
	int max = -1, i, j;
	for (i = 0; i < n; i++)
		max = std::max(max, niz[i]);

	max += 1;
	std::vector<node*> Bin(max, nullptr);

	for (i = 0; i < n; i++)
	{
		node* t = new node(A[i]);
		t->next = Bin[A[i]];
		Bin[A[i]] = t;
	}

	i = 0, j = 0;
	while (i < max)
	{
		if (Bin[i])
		{
			node* t = Bin[i];
			niz[j++] = t->val; // ili niz[j++] = i
			Bin[i] = Bin[i]->next;
			delete t;
		}
		else
			i++;
	}
}

void Display(int niz[], int n)
{
	for (int i = 0; i < n; i++)
		std::cout << niz[i] << " ";
	std::cout << std::endl;
}

int main()
{
	int niz[] = { 5, 8, 4, 2, 3, 7, 6, 1, 0, 9, 2 };
	int n = std::size(niz);
	Bin_Bucket_Sort(niz, n);
	Display(niz, n);
}